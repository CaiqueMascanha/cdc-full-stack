export interface EmprestimoSolicitado {
  data_solicitado: string;
  total_emprestimos_solicitados: number;
  total_valor_solicitado: number;
}

export interface EmprestimoAprovado {
  data_aprovado: string;
  total_emprestimos_aprovados: number;
  total_valor_aprovado: number;
}
